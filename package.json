{
    "name": "project101-monorepo",
    "version": "1.0.0",
    "license": "MIT",
    "scripts": {
        "build:preview_card_component": "npm --prefix preview_card_component run build",
        "build:single_price_master_component": "npm --prefix single_price_master_component run build",
        "build:feature_section": "npm --prefix feature_section run build"
    }
}
