import reactIcons from "../assets/react.svg";

const Tag = () => {
    return (
        <div className="tag">
            <img src={reactIcons} alt="react icon for tag" />
            <p>R E A C T</p>
        </div>
    );
};

export default Tag;
